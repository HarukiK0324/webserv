## インターフェース部分について

- CgiHandler
- CgiParser
- ConnectionHander

### CGI Handler 環境変数作成

``` cpp
CgiHandler(LocationConfig &lconf, ServerLoop *loop);
void MakeEnvp(std::string &startline, std::map< std::string, std::string > &headers, std::vector<char> &body, std::string &client_ip, std::string &port);
```


入力　
- パスの特定ののち、LocationConfigの指定

- startline
	- クエリ
	- メソッド
	- HTTP　VER
	- targetURI

### CGIPARSER

出口
- Syntax OK　→　無加工でデータ全て送る
- Syntax NG　→　Enum でステータスコード取得


### ConectionHandler

```cpp
std::vector<char>　
```